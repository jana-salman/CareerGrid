import { useState } from 'react'
function GitHubApp({ git }) { const [title, setTitle] = useState(''); const [body, setBody] = useState(''); const [pullRequest, setPullRequest] = useState(null); return <div className="github-layout"><header className="github-repository-header"><h2>CareerGrid workspace repository</h2><span>{git.branch}</span></header><section className="github-content"><h3>Branches</h3><p>{git.pushed ? `${git.branch} pushed` : 'Push a branch from Terminal to create a pull request.'}</p><h3>Commits</h3><ul>{git.commits.map((commit) => <li key={commit}>{commit}</li>)}</ul>{git.pushed && !pullRequest && <form className="github-pr-form" onSubmit={(event) => { event.preventDefault(); if (title.trim()) setPullRequest({ title, body }) }}><input placeholder="Pull request title" value={title} onChange={(event) => setTitle(event.target.value)} /><textarea placeholder="Describe your changes" value={body} onChange={(event) => setBody(event.target.value)} /><button type="submit">Create pull request</button></form>}{pullRequest && <article className="github-pull-request"><h3>{pullRequest.title}</h3><p>{pullRequest.body}</p><strong>Pull request created</strong></article>}</section></div> }
export default GitHubApp
