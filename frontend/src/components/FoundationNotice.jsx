function FoundationNotice({ title = 'CareerGrid' }) {
  return (
    <section aria-labelledby="foundation-title">
      <h1 id="foundation-title">{title}</h1>
      <p>This route is prepared for a future React migration.</p>
    </section>
  )
}

export default FoundationNotice
